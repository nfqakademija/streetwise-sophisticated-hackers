<h4>{{ 'student'|trans(_trans_parameters, 'EasyAdminBundle') }}</h4>
<div class="table-responsive">
    <table class="table custom-table">
        <thead>
        <tr>
            <th class="string">
                {{ 'user.name'|trans(_trans_parameters, 'EasyAdminBundle') }}
            </th>
            <th class="datetime">
                {{ 'user.email'|trans(_trans_parameters, 'EasyAdminBundle') }}
            </th>
            <th>
                <span>{{ 'actions'|trans(_trans_parameters, 'EasyAdminBundle') }}</span>
            </th>
        </tr>
        </thead>
        <tbody>
        {% for item in items %}
            <tr>
                <td class="string">
                    {% if is_granted('show', item) %}
                    <a class="action-show" href="{{ path('easyadmin', {'action': 'show', 'entity': 'User', 'id': item.id}) }}">
                        {{ item.name }}
                    </a>
                    {% else %}
                        {{ item.name }}
                    {% endif %}
                </td>
                <td class="string">
                    {{ item.email }}
                </td>
                <td class="actions">
                    {% if is_granted('edit', item) %}
                        <a class="action-edit" href="{{ path('easyadmin', {'action': 'edit', 'entity': 'User', 'id': item.id}) }}">
                            {{ 'edit.action'|trans(_trans_parameters, 'EasyAdminBundle') }}
                        </a>
                    {% endif %}
                    {% if is_granted('delete', item) %}
                        <a class="action-delete" href="{{ path('easyadmin', {'action': 'delete', 'entity': 'User', 'id': item.id}) }}">
                            {{ 'delete_modal.action'|trans(_trans_parameters, 'EasyAdminBundle') }}
                        </a>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
