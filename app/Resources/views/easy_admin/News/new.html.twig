{% extends '@EasyAdmin/default/new.html.twig' %}

{% block entity_form %}

    {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn btn-default"> {{ 'submit'|trans(_trans_parameters, 'EasyAdminBundle') }} </button>
    {{ form_end(form) }}

{% endblock entity_form %}
