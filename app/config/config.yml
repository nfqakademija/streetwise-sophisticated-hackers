imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: en
    app.path.lecture_slides: /uploads/files/lecture/slides
    app.path.homework_assignments: /uploads/files/homework/assignments

framework:
    #esi: ~
    translator: { fallbacks: ['%locale%'] }
    secret: '%secret%'
    router:
        resource: '%kernel.root_dir%/config/routing.yml'
        strict_requirements: ~
    form: ~
    csrf_protection: ~
    validation: { enable_annotations: true }
    #serializer: { enable_annotations: true }
    templating:
        engines: ['twig']
    default_locale: '%locale%'
    trusted_hosts: ~
    trusted_proxies: ~
    session:
        # http://symfony.com/doc/current/reference/configuration/framework.html#handler-id
        handler_id:  session.handler.native_file
        save_path:   "%kernel.root_dir%/../var/sessions/%kernel.environment%"
    fragments: ~
    http_method_override: true
    assets: ~
    php_errors:
        log: true

# Twig Configuration
twig:
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    paths:
      '%kernel.root_dir%/Resources': AppBundle

# Doctrine Configuration
doctrine:
    dbal:
        driver: pdo_mysql
        host: '%database_host%'
        port: '%database_port%'
        dbname: '%database_name%'
        user: '%database_user%'
        password: '%database_password%'
        charset: UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/../var/data/data.sqlite"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #path: '%database_path%'

    orm:
        auto_generate_proxy_classes: '%kernel.debug%'
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: '%mailer_transport%'
    host: '%mailer_host%'
    username: '%mailer_user%'
    password: '%mailer_password%'
    spool: { type: memory }

doctrine_migrations:
    dir_name: "%kernel.root_dir%/DoctrineMigrations"
    namespace: Application\Migrations
    table_name: migration_versions
    name: Application Migrations

fos_user:
    db_driver: orm # other valid values are 'mongodb' and 'couchdb'
    firewall_name: main
    user_class: AppBundle\Entity\User
    from_email:
        address: "%mailer_user%"
        sender_name: "%mailer_user%"

vich_uploader:
    db_driver: orm
    mappings:
        lecture_slides:
            uri_prefix: '%app.path.lecture_slides%'
            upload_destination: '%kernel.root_dir%/Resources/uploads/files/lecture/slides'
            namer:              vich_uploader.namer_origname
        work_file:
            uri_prefix: '%app.path.homework_assignments%'
            upload_destination: '%kernel.root_dir%/Resources/uploads/files/homework/assignments'
            namer:              vich_uploader.namer_origname

fos_message:
    db_driver: orm
    thread_class: AppBundle\Entity\Thread
    message_class: AppBundle\Entity\Message

easy_admin:
    site_name: '<img src="/assets/img/nfq.png" />'
    design:
        menu:
            - { label: 'Messenger', url: '/messages' }
            - News
            - Lecture
            - Homework
            - User
            - Comment
        brand_color: '#000000'
    entities:
        User:
            controller: AppBundle\Controller\Admin\UserController
            class: AppBundle\Entity\User
            form:
                fields:
                    - username
                    - name
                    - { property: 'email', type: 'email', label: 'Email' }
                    - enabled
                    - { property: 'roles', type: 'choice', type_options: { multiple: true, choices: { 'ROLE_USER': 'ROLE_USER', 'ROLE_LECTOR': 'ROLE_LECTOR', 'ROLE_ADMIN': 'ROLE_ADMIN' } } }
            edit:
                form_options: { validation_groups: ['Default'] }
                fields:
                    # if administrators are allowed to edit users' passwords and roles, add this:
                    - { property: 'plainPassword', label: 'New password', type: 'password', type_options: { required: false } }
                    - { property: 'confirmPassword', label: 'Confirm password', type: 'password', type_options: { required: false } }
            new:
                form_options: { validation_groups: ['Default', 'registration'] }
                fields:
                    # if administrators are allowed to edit users' passwords and roles, add this:
                    - { property: 'plainPassword', label: 'Password', type: 'password', type_options: { required: true } }
                    - { property: 'confirmPassword', label: 'Confirm password', type: 'password', type_options: { required: false } }
            list:
                actions: [{name: 'show', roles: ['ROLE_USER'] }, { name: 'edit', roles: ['ROLE_ADMIN'] }, { name: 'delete', roles: ['ROLE_ADMIN'] }, { name: 'new', roles: ['ROLE_ADMIN'] }]
                fields:
                    - name
                    - email
                    - roles
            show:
                actions: [ { name: 'edit', roles: ['ROLE_ADMIN'] }, { name: 'delete', roles: ['ROLE_ADMIN'] }, { name: 'list', roles: ['ROLE_USER'] } ]
                fields:
                    - username
                    - name
                    - email
                    - enabled
                    - roles
        Lecture:
            controller: AppBundle\Controller\Admin\LectureController
            class: AppBundle\Entity\Lecture
            form:
                fields:
                    - title
                    - description
                    - date
                    - lecturer
            edit:
                form_options: { validation_groups: ['Default'] }
                fields:
                    - { property: 'slidesFile', type: 'vich_file' }
            new:
                form_options: { validation_groups: ['registration'] }
            list:
                sort: [ 'date', 'ASC' ]
                actions: [ {name: 'show', roles: ['ROLE_USER'] }, { name: 'edit', roles: ['ROLE_LECTOR'] }, { name: 'delete', roles: ['ROLE_ADMIN'] }, { name: 'new', roles: ['ROLE_ADMIN'] } ]
                fields:
                    - title
                    - date
                    - lecturer
            show:
                actions: [ { name: 'edit', roles: ['ROLE_LECTOR'] }, { name: 'delete', roles: ['ROLE_ADMIN'] }, { name: 'list', roles: ['ROLE_USER'] } ]
                fields:
                    - title
                    - description
                    - date
                    - lecturer
                    - { property: 'slides', template: 'slides.html.twig', base_path: "/admin/download/lecture" }
        Homework:
            controller: AppBundle\Controller\Admin\HomeworkController
            class: AppBundle\Entity\Homework
            form:
                fields:
                    - title
                    - description
                    - dueDate
                    - lecturer
            edit:
                form_options: { validation_groups: ['Default'] }
            new:
                form_options: { validation_groups: ['registration'] }
            list:
                sort: [ 'dueDate', 'ASC' ]
                actions: [ {name: 'show', roles: ['ROLE_USER'] }, { name: 'edit', roles: ['ROLE_LECTOR'] }, { name: 'delete', roles: ['ROLE_LECTOR'] }, { name: 'new', roles: ['ROLE_LECTOR'] }]
                fields:
                    - title
                    - dueDate
                    - lecturer
            show:
                actions: [ { name: 'edit', roles: ['ROLE_LECTOR'] }, { name: 'delete', roles: ['ROLE_LECTOR', 'ROLE_ADMIN'] }, { name: 'list', roles: ['ROLE_USER'] } ]
                fields:
                    - title
                    - description
                    - dueDate
                    - lecturer

        Assignment:
            controller: AppBundle\Controller\Admin\AssignmentController
            class: AppBundle\Entity\Assignment
            disabled_actions: ['new', 'list', 'edit', 'delete']
            show:
                fields:
                    - homework
                    - student
                    - date
                    - { property: 'work', template: 'work.html.twig', base_path: "/admin/download/homework" }
                    - grade
                    - { property: 'slides', template: 'slides.html.twig', base_path: '%app.path.lecture_slides%' }
        News:
            controller: AppBundle\Controller\Admin\NewsController
            class: AppBundle\Entity\News
            form:
                fields:
                    - title
                    - description
                    - date
                    - author
            edit:
                form_options: { validation_groups: ['Default'] }
            new:
                form_options: { validation_groups: ['registration'] }
            list:
                sort: [ 'date', 'ASC' ]
                actions: [{name: 'show', roles: ['ROLE_USER']}, { name: 'edit', roles: ['ROLE_LECTOR'] }, { name: 'delete', roles: ['ROLE_ADMIN'] }, { name: 'new', roles: ['ROLE_ADMIN'] }]
                fields:
                    - title
                    - date
                    - author
            show:
                actions: [ { name: 'edit', roles: ['ROLE_LECTOR', 'ROLE_ADMIN'] }, { name: 'delete', roles: ['ROLE_ADMIN'] }, { name: 'list', roles: ['ROLE_USER'] } ]
                fields:
                    - title
                    - description
                    - date
                    - author
        Comment:
            controller: AppBundle\Controller\Admin\CommentController
            class: AppBundle\Entity\Comment
            form:
                fields:
                    - content
                    - date
                    - author
            edit:
                form_options: { validation_groups: ['Default'] }
            new:
                form_options: { validation_groups: ['registration'] }
            list:
                sort: [ 'date', 'ASC' ]
                actions: [{name: 'show', roles: ['ROLE_USER']}, { name: 'edit', roles: ['ROLE_ADMIN'] }, { name: 'delete', roles: ['ROLE_ADMIN'] }, { name: 'new', roles: ['ROLE_ADMIN'] }]
                fields:
                    - newsId
                    - content
                    - author
                    - date
            show:
                actions: [ { name: 'list', roles: ['ROLE_USER'] }, { name: 'edit', roles: ['ROLE_ADMIN'] }, { name: 'delete', roles: ['ROLE_ADMIN'] } ]
                fields:
                    - newsId
                    - content
                    - author
                    - date
